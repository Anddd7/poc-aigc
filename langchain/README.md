# langchain prompts marketplace

write template in markdown,
load template in python, expose variables as method parameters,
call and render templates in notebook

template

- role: background, senario
- task: goal, step
